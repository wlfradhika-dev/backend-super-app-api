info:
  name: Accept Request
  type: http
  seq: 1

http:
  method: POST
  url: "{{transport_url}}/api/driver/taxi/accept-request"
  body:
    type: multipart-form
    data:
      - name: service_category_id
        type: text
        value: "2"
        description: "service category id "
      - name: request_id
        type: text
        value: e96b9c62-3c53-41c7-bdcf-a8d0cd99a370
        description: Enter ride booking id
  auth:
    type: bearer
    token: "{{driver_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Accept Request
    request:
      url: "{{transport_url}}/api/driver/taxi/accept-request"
      method: POST
      body:
        type: multipart-form
        data:
          - name: driver_id
            type: text
            value: "{{driver_id}}"
            description: For Driver id
          - name: service_category_id
            type: text
            value: "2"
            description: "service category id "
          - name: request_id
            type: text
            value: e96b9c62-3c53-41c7-bdcf-a8d0cd99a370
            description: Enter ride booking id
    response:
      status: 400
      statusText: Bad Request
      headers:
        - name: Host
          value: 127.0.0.1:8001
        - name: Connection
          value: close
        - name: X-Powered-By
          value: PHP/8.5.2
        - name: Cache-Control
          value: no-cache, private
        - name: Date
          value: Tue, 10 Feb 2026 10:55:57 GMT
        - name: Content-Type
          value: application/json
        - name: Access-Control-Allow-Origin
          value: "*"
      body:
        type: json
        data: |-
          {
              "success": false,
              "message": "Uh-oh! Too Late to Accept Ride",
              "message_code": 24,
              "status_code": 0,
              "https_status_code": 400,
              "errors": "messages.2"
          }

docs: |+
  ## Accept Request

  Performs the requested operation and returns the result.

  ### Authentication

  Requires a valid **Bearer Token** in the `Authorization` header.

  ```
  Authorization: Bearer {token}
  ```

  ---

  ### Request Body (form-data)

  | Field | Type | Required | Description |
  |-------|------|----------|-------------|
  | `service_category_id` | string | Yes | service category id  |
  | `request_id` | string | Yes | Enter ride booking id |

  ---

  ### Responses

  **200 OK — Success**

  ```json
  {
      "success": true,
      "message": "Success",
      "message_code": 1,
      "status_code": 1,
      "http_status_code": 200
  }
  ```

  **401 Unauthorized — App Access Denied**

  ```json
  {
      "success": false,
      "message": "App access denied",
      "message_code": 2,
      "status_code": 0,
      "https_status_code": 401,
      "errors": "Failed"
  }
  ```

  **422 Unprocessable Entity — Validation Failed**

  ```json
  {
      "status": 0,
      "message": "The service category id field is required."
  }
  ```

  **404 Not Found — User Not Found**

  ```json
  {
      "success": false,
      "message": "App User Not Found",
      "message_code": 5,
      "status_code": 5,
      "https_status_code": 404,
      "errors": "Failed"
  }
  ```

