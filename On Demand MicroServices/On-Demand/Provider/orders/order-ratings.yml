info:
  name: order-ratings
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8004/api/provider/on-demand/orders/order-rating
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
      disabled: true
  body:
    type: multipart-form
    data:
      - name: order_id
        type: text
        value: "{{ORDER_UUID}}"
      - name: rating
        type: text
        value: "4.5"
      - name: comment
        type: text
        value: Great customer, very cooperative
  auth:
    type: bearer
    token: "{{provider_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Success-Response
    request:
      url: http://localhost:8004/api/provider/on-demand/orders/order-rating
      method: POST
      headers:
        - name: Accept
          value: application/json
        - name: Content-Type
          value: application/json
          disabled: true
      body:
        type: multipart-form
        data:
          - name: order_id
            type: text
            value: "{{ORDER_UUID}}"
          - name: rating
            type: text
            value: "4.5"
          - name: comment
            type: text
            value: Great customer, very cooperative
    response:
      status: 200
      statusText: OK
      headers:
        - name: cache-control
          value: no-cache, private
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
              "success": true,
              "message": "Success",
              "message_code": 1,
              "status_code": 1,
              "http_status_code": 200
          }

docs: |+
  ## order-ratings

  Performs the requested operation and returns the result.

  ### Authentication

  Requires a valid **Bearer Token** in the `Authorization` header.

  ```
  Authorization: Bearer {token}
  ```

  ---

  ### Request Body (form-data)

  | Field | Type | Required | Description |
  |-------|------|----------|-------------|
  | `order_id` | string | Yes | Order id |
  | `rating` | string | Yes | Rating |
  | `comment` | string | Yes | Comment |

  ---

  ### Responses

  **200 OK — Success**

  ```json
  {
      "success": true,
      "message": "Success",
      "message_code": 1,
      "status_code": 1,
      "http_status_code": 200
  }
  ```

  **401 Unauthorized — App Access Denied**

  ```json
  {
      "success": false,
      "message": "App access denied",
      "message_code": 2,
      "status_code": 0,
      "https_status_code": 401,
      "errors": "Failed"
  }
  ```

  **422 Unprocessable Entity — Validation Failed**

  ```json
  {
      "status": 0,
      "message": "The order id field is required."
  }
  ```

  **404 Not Found — User Not Found**

  ```json
  {
      "success": false,
      "message": "App User Not Found",
      "message_code": 5,
      "status_code": 5,
      "https_status_code": 404,
      "errors": "Failed"
  }
  ```

