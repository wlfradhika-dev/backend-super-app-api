info:
  name: Cancel
  type: http
  seq: 1

http:
  method: POST
  url: "{{core_url}}/api/provider/subscription-plan/cancel"
  headers:
    - name: Accept
      value: application/json
  body:
    type: multipart-form
    data:
      - name: subscription_id
        type: text
        value: 1cb73889-9de3-49b6-a586-aa9322eb47ef
  auth:
    type: bearer
    token: "{{provider_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Cancel
    request:
      url: "{{core_url}}/api/provider/subscription-plan/cancel"
      method: POST
      headers:
        - name: Accept
          value: application/json
      body:
        type: multipart-form
        data:
          - name: subscription_id
            type: text
            value: 1cb73889-9de3-49b6-a586-aa9322eb47ef
    response:
      status: 200
      statusText: OK
      headers:
        - name: Host
          value: 127.0.0.1:8000
        - name: Connection
          value: close
        - name: X-Powered-By
          value: PHP/8.3.14
        - name: Cache-Control
          value: no-cache, private
        - name: Date
          value: Thu, 19 Feb 2026 07:19:12 GMT
        - name: Content-Type
          value: application/json
        - name: Access-Control-Allow-Origin
          value: "*"
      body:
        type: json
        data: |-
          {
              "success": true,
              "message": "Subscription plan cancelled successfully!",
              "message_code": 6,
              "status_code": 1,
              "http_status_code": 200
          }

docs: |+
  ## Cancel

  Manages subscription plans including listing, history, and cancellation for the authenticated user.

  ### Authentication

  Requires a valid **Bearer Token** in the `Authorization` header.

  ```
  Authorization: Bearer {token}
  ```

  ---

  ### Request Body (form-data)

  | Field | Type | Required | Description |
  |-------|------|----------|-------------|
  | `subscription_id` | string | Yes | Subscription id |

  ---

  ### Responses

  **200 OK — Success**

  ```json
  {
      "success": true,
      "message": "Subscription plan cancelled successfully!",
      "message_code": 6,
      "status_code": 1,
      "http_status_code": 200
  }
  ```

  **401 Unauthorized — App Access Denied**

  ```json
  {
      "success": false,
      "message": "App access denied",
      "message_code": 2,
      "status_code": 0,
      "https_status_code": 401,
      "errors": "Failed"
  }
  ```

  **422 Unprocessable Entity — Validation Failed**

  ```json
  {
      "status": 0,
      "message": "The subscription id field is required."
  }
  ```

  **404 Not Found — User Not Found**

  ```json
  {
      "success": false,
      "message": "App User Not Found",
      "message_code": 5,
      "status_code": 5,
      "https_status_code": 404,
      "errors": "Failed"
  }
  ```

